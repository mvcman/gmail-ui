import React from 'react';
import useStyles from './styles';

export default function Rightebar(){
    const classes = useStyles();
    return (
        <div className={classes.rightbar}>
            
        </div>
    );
}